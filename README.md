This is an exercise in creating an Electron application for viewing the graph
of commits in a project using __git__ for scc.

`npm start` runs the app.

`mocha` runs regression tests.

Work in progress.
